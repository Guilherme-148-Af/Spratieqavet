'use strict';

/**
 * relatorio-verificacao router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::relatorio-verificacao.relatorio-verificacao');
