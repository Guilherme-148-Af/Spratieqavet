'use strict';

/**
 * relatorio-verificacao controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::relatorio-verificacao.relatorio-verificacao');
